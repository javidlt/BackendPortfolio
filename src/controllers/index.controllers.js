const defaultCtrl = {};
defaultCtrl.defaultFunc = (req, res) => {
    res.send('Hola, mi nombre es Francisco Javier de la Torre Silva. Este es el api que realice para tener un registro de los proyectos que he elaborado y elaboraré. 👋')
}

module.exports = defaultCtrl;